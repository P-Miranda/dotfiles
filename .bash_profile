############################################
# .bash_profile file
#
# This file is executed from the bash shell
# when you log in.
############################################

# source .profile if exists
if [ -f $HOME/.profile ]; then
	source $HOME/.profile
fi
